# Skill-HQE Development & Testing Dependencies
# Pinned to exact versions so CI resolution is deterministic: a future release
# cannot change CI behavior without an explicit repository commit.
pytest==9.0.3
jsonschema==4.26.0
referencing==0.37.0
PyYAML==6.0.3
